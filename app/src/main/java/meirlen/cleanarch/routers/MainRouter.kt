package meirlen.cleanarch.routers

import android.content.Context


class MainRouter {

    fun showColumns(context: Context?, id: String) {
       // context?.let { it.startActivity(ColumnsActivity.getStartIntent(it, id)) }
    }
}